{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Katedra list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nazwa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                        <td>{{ loop.index }}.</td>
           <td>{{ entity.name }}</td>
            <td>
                <a href="{{ path('admin_katedra_show', { 'id': entity.id }) }}">
                   {{ entity.nazwa }}
                </a>
           </td>
                
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_katedra_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_katedra_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_katedra_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
